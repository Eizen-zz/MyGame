#ifndef _GAME_TYPES_H
#define _GAME_TYPES_H

const float WindowWidth = 800;
const float WindowHeight = 800;
const float RacketWidth = (WindowWidth / 4) + (WindowWidth / 40);
const float RacketHeight = (WindowHeight / 40) - (WindowHeight / 400);
const int Time = 1;

typedef unsigned int uint;

#endif